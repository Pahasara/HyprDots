# size-mtime.yazi

A minimal Yazi linemode plugin that displays **file size** and **last modified time**, similar to `ls -l`.

## Installation

```sh
ya pkg add pahasara/size-mtime
````

## Usage

Set the linemode in your Yazi config:

```toml
[manager]
linemode = "size_and_mtime"
```

