# ──────────────────────────────────────────────────────────────
# CORE
# ──────────────────────────────────────────────────────────────
scan_timeout = 5
command_timeout = 300
add_newline = true

# ──────────────────────────────────────────────────────────────
# PROMPT LAYOUT
# ──────────────────────────────────────────────────────────────
format = """
[](fg:#323437)\
[  ](bg:#363842 fg:#CFCFCF)\
[](bg:#008FCF fg:#323437)\
[ $directory ](bg:#008FCF fg:#000000)\
[ ](fg:#008FCF)\
$line_break\
"""

right_format = """
$character\
$symbol\
$c\
$cpp\
$rust\
$dotnet\
$java\
$python\
$nodejs\
$git_branch\
$git_state\
$git_status\
$cmd_duration\
"""

# ──────────────────────────────────────────────────────────────
# CHARACTER
# ──────────────────────────────────────────────────────────────
[character]
success_symbol = "[✔](green)"
error_symbol = "[✘](red)"
vimcmd_symbol = "[λ](blue)"

# ──────────────────────────────────────────────────────────────
# USERNAME & HOSTNAME
# ──────────────────────────────────────────────────────────────
[username]
show_always = true
style_user = "bg:color_orange fg:color_fg0"
style_root = "bg:color_orange fg:color_fg0"
format = '[ $user]($style)'

[hostname]
ssh_only = false
format = '[$hostname ]($style)'
style = 'bold gray'

# ──────────────────────────────────────────────────────────────
# DIRECTORY
# ──────────────────────────────────────────────────────────────
[directory]
format = '[$path]($style)[$read_only]($read_only_style)'
home_symbol = "~"
truncation_length = 4
truncation_symbol = "…/"
truncate_to_repo = true
style = 'bold gray'
read_only = " 󰌾"
read_only_style = "bg:#008FCF fg:#000000"

[directory.substitutions]
"Desktop" = "󰇄 "
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Code" = " "
"Pictures" = " "

# ──────────────────────────────────────────────────────────────
# COMMAND DURATION
# ──────────────────────────────────────────────────────────────
[cmd_duration]
format = "[](fg:#E9A127)[ $duration ]($style)[](fg:#E9A127)"
style = "fg:000000 bg:#E9A127"
min_time = 1
show_milliseconds = true

# ──────────────────────────────────────────────────────────────
# SHELL INDICATOR
# ──────────────────────────────────────────────────────────────
[shell]
zsh_indicator = '_'
style = "fg:#000000 bg:#45BB11"
format = '[](fg:#45BB11)[ $indicator ]($style)[](fg:#45BB11)'
disabled = false

# ──────────────────────────────────────────────────────────────
# PROGRAMMING LANGUAGES
# ──────────────────────────────────────────────────────────────
# Style: [Symbol ](BG_COLOR) [ Version ](Style) [](BG_COLOR)

[c]
symbol = ''
style = "fg:#000000 bg:#127CFF"
format = '[$symbol ](fg:#127CFF)[ $version ]($style)[](fg:#127CFF)'

[cpp]
symbol = ''
style = "fg:#000000 bg:#5797D8"
format = '[$symbol ](fg:#5797D8)[ $version ]($style)[](fg:#5797D8)'
disabled = false

[dotnet]
symbol = ''
style = "fg:#000000 bg:#997DF8"
format = '[$symbol ](fg:#997DF8)[ $version ]($style)[](fg:#997DF8)'

[java]
symbol = ''
style = "fg:#000000 bg:#FD3502"
format = '[$symbol ](fg:#FD3502)[ $version ]($style)[](fg:#FD3502)'

[python]
symbol = ''
style = "fg:#000000 bg:#18C429"
format = '[$symbol ](fg:#18C429)[ $virtualenv $version ]($style)[](fg:#18C429)'

[rust]
symbol = '🦀'
style = "fg:#000000 bg:#CE412B"
format = '[$symbol ](fg:#CE412B)[ $version ]($style)[](fg:#CE412B)'

[nodejs]
symbol = ''
style = "fg:#000000 bg:#54B538"
format = '[$symbol ](fg:#54B538)[ $version ]($style)[](fg:#54B538)'

# ──────────────────────────────────────────────────────────────
# GIT MODULES
# ──────────────────────────────────────────────────────────────
[git_branch]
symbol = ""
style = "fg:#000000 bg:#18C429"
format = '[](fg:#18C429)[ $symbol $branch ]($style)[](fg:#18C429  bg:#FF3F3F)'
truncation_length = 20
only_attached = false

[git_status]
style = "fg:#000000 bg:#FF3F3F"
format = '[$all_status$ahead_behind ]($style)[](fg:#FF3F3F)'
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
conflicted = "=${count}"
deleted = "✘${count}"
renamed = "»${count}"
modified = "!${count}"
staged = "+${count}"
untracked = "?${count}"
stashed = '$${count}'

[git_state]
format = '[](fg:yellow)[$state( $progress_current of $progress_total)]($style)[](fg:yellow)'
style = "bold yellow"

# ──────────────────────────────────────────────────────────────
# PERFORMANCE DISABLES
# ──────────────────────────────────────────────────────────────
[git_metrics]
disabled = true

[git_commit]
disabled = true

[time]
disabled = true

[package]
disabled = true

[aws]
disabled = true

[gcloud]
disabled = true

[docker_context]
disabled = true

[kubernetes]
disabled = true
